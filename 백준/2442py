#오른쪽 공백은 런타임 에러 발생할 수 있어!

import sys

sys.stdin = open('input.txt', 'r')


n = int(sys.stdin.readline())

star_max = 2* n - 1

for i in range(1, n+1):
    star_length = 2*i -1
    padding = (star_max - star_length) // 2


    print(' '* padding + '*' * star_length)